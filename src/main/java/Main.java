public class Main {
    public static void main(String[] args) {
        //The main function only serves one purpose and that is to start the program and create an object called run
        //"Run" is where the application actually starts.
        Run run = new Run();
        run.run();
    }
}
